/*demonstrate module partition works*/
#include <iostream>
import mathx;
int main() {
  std::cout << "add(x,y) : " << add(2, 2) << std::endl;
  std::cout << "mul(x,y) : " << mul(12, 4) << std::endl;
  return 0;
}